<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.liuwanwan.accountbook">
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:icon="@drawable/ic_cost_type_0"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_cost_type_0"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.AddRecordActivity"
            android:windowSoftInputMode="adjustPan"
            android:launchMode="singleTask">
        </activity>
        <activity android:name=".activity.BudgetActivity"
            android:windowSoftInputMode="adjustPan"
            android:launchMode="singleTask">
        </activity>
        <activity android:name=".activity.ClassStatisticActivity"
            android:launchMode="singleTask">
        </activity>
        <activity android:name=".activity.AddAssetActivity"
            android:launchMode="singleTask">
        </activity>
    </application>

</manifest>